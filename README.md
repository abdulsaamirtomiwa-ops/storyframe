# StoryFrame

Image-first story website with genre filters, search, reader modal, and local story creation. The supplied images are included in the downloadable package; place them in `images/` when deploying the repository.